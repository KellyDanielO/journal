import 'package:flutter/material.dart';

class MobileSavedWidget extends StatelessWidget {
  const MobileSavedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}